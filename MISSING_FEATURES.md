# Отсутствующий функционал из Go проекта

## Сравнение функционала Go (gestalt) и Django (kpalch)

### ✅ Перенесено в Django:

1. **Базовые операции со списками:**
   - ✅ GET /list - получение списка
   - ✅ POST /add - добавление элемента
   - ✅ PUT /buy/{name} - отметка как купленного
   - ✅ DELETE /delete/{name} - удаление элемента
   - ✅ PUT /edit/{name} - редактирование элемента
   - ✅ POST /reorder - изменение порядка элементов

2. **Внутренние API для сервисов:**
   - ✅ GET /internal/api/list
   - ✅ POST /internal/api/add
   - ✅ PUT /internal/api/buy/{name}
   - ✅ DELETE /internal/api/delete/{name}
   - ✅ PUT /internal/api/edit/{name}

3. **Функции друзей:**
   - ✅ GET /api/users/all - получение всех пользователей
   - ✅ GET /api/users/search - поиск пользователей
   - ✅ GET /api/friends - получение списка друзей
   - ✅ POST /api/friends/add - добавление друга (в Go было простое добавление)
   - ✅ DELETE /api/friends/remove - удаление друга
   - ✅ **Улучшено в Django:** система запросов на дружбу (pending/accepted/rejected)

4. **Шаринг списков:**
   - ✅ POST /api/share-list - поделиться списком
   - ✅ GET /api/shared-lists - получить списки, которыми поделились
   - ✅ **Улучшено в Django:** система принятия/отклонения шаринга

5. **Категории:**
   - ✅ POST /api/category/add - добавление категории
   - ✅ DELETE /api/category/delete/{name} - удаление категории
   - ✅ Проверка системных категорий (нельзя удалять)

6. **OAuth авторизация:**
   - ✅ Авторизация через Yandex
   - ✅ Callback обработка
   - ✅ Сессии

### ❌ Отсутствует в Django:

1. **GET /api/user** - получение информации о текущем пользователе
   - В Go: `/api/user` возвращает `{id, name, email}`
   - В Django: нет такого endpoint

2. **Drag and Drop для изменения порядка** (в фронтенде)
   - В Go: есть drag and drop в index.html
   - В Django: есть POST /reorder, но drag and drop в шаблоне может отсутствовать

3. **Массовое удаление купленных элементов** (UI)
   - В Go: есть кнопка "Удалить выбранное" для нескольких элементов
   - В Django: есть функционал, но нужно проверить UI

### 🔄 Отличия в реализации:

1. **Друзья:**
   - Go: простое двустороннее добавление через Redis Sets
   - Django: система запросов на дружбу с статусами (pending/accepted/rejected) - **улучшение**

2. **Шаринг списков:**
   - Go: можно делиться только списком "купить", простое копирование в Redis
   - Django: можно делиться любой категорией, система принятия/отклонения - **улучшение**

3. **Внутренние API:**
   - Go: проверка IP адреса через middleware
   - Django: проверка через middleware на основе IP или заголовка X-Internal-Request

4. **Категории:**
   - Go: жестко заданы 5 категорий в коде
   - Django: динамические категории в БД, но есть проверка системных (5 стандартных)

### 📝 Рекомендации:

1. **Добавить GET /api/user** endpoint для получения информации о текущем пользователе
2. **Проверить drag and drop** в шаблоне index.html
3. **Проверить массовое удаление** в UI
4. **Обновить список стандартных категорий** - должно быть 5: 'купить', 'не-забыть', 'дом', 'машина', 'холодос'

